[package]
name = "databend-meta-base"
version = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
publish = { workspace = true }
edition = { workspace = true }

[dependencies]
anyerror = { workspace = true }
deepsize = { workspace = true }
derive_more = { workspace = true }
display-more = { workspace = true }
map-api = { workspace = true }
# `Change` derives serde on `SeqV`, whose impls sit behind the `seqv-serde`
# feature. Declaring the dependency is what turns that feature on for a build
# of this crate alone; a workspace-wide build got it from a sibling crate.
seq-marked = { workspace = true }
serde = { workspace = true }
state-machine-api = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
anyhow = { workspace = true }
rmp-serde = { workspace = true }
serde_json = { workspace = true }

[package.metadata.cargo-machete]
# Depended on for its `seqv-serde` feature, not for anything this crate names.
ignored = ["seq-marked"]

[lints]
workspace = true
